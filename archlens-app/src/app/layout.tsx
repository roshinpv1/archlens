import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "CloudArc - Cloud Architecture Review System",
  description: "AI-powered cloud architecture analysis and review system supporting multiple cloud platforms",
  icons: {
    icon: "/cloudarc-logo.svg",
    shortcut: "/cloudarc-logo.svg",
    apple: "/cloudarc-logo.svg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
